<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ovkdesktop.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ovkdesktop"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="ovkdesktop">

    <Grid x:Name="RootGrid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <!-- Main content -->
        <Frame x:Name="ContentFrame" Grid.Row="0" />
        
        <!-- Mini player will be added to this container dynamically -->
        <Grid x:Name="MiniPlayerContainer" Grid.Row="1" HorizontalAlignment="Stretch" />
    </Grid>
</Window>
