<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ovkdesktop.TypeNewPostPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ovkdesktop"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <TextBlock x:Name="NewPostMPLabel" Text="Новый пост" Margin="125,105,0,0" Style="{ThemeResource TitleLargeTextBlockStyle}" FontSize="42" ></TextBlock>
            <TextBlock x:Name="ErrorTextBlock" Margin="250,190,0,0" Foreground="Red" Visibility="Collapsed" TextWrapping="Wrap"/>
        </Grid>

        <Grid Grid.Row="1" Margin="120,40,40,50">
            <DropDownButton AnimatedIcon.State="Add" Content="Добавить" Margin="0,-420,0,0" x:Name="MediaAddNewPostButton">
                <DropDownButton.Flyout>
                    <MenuFlyout Placement="Bottom">
                        <MenuFlyoutItem Text="Фото" Click="UploadPhotoButton">
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE91B;"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem Text="Видео">
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xF131;"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem Text="Музыка">
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE8D6;"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                    </MenuFlyout>
                </DropDownButton.Flyout>
            </DropDownButton>

            <TextBox AutomationProperties.Name="multi-line TextBox" x:Name="NewPostTextBox"
                TextWrapping="Wrap" AcceptsReturn="True" IsSpellCheckEnabled="True"
                Width="700" MinHeight="300" MaxHeight="300" Margin="-670,-40,0,0" />

            <Image x:Name="SelectedImagePreview" 
               MaxHeight="300" 
               Stretch="Uniform"
               Margin="0,10,0,0"/>

            <Button Content="Опубликовать" Margin="0,380,0,0" Click="PublishNewPostButton"/>
            <Button Content="Отмена" Margin="140,380,0,0"/>
        </Grid>
        <Frame x:Name="ContentNewPostFrame">
            <Frame.ContentTransitions>
                <TransitionCollection>
                    <NavigationThemeTransition  />
                </TransitionCollection>
            </Frame.ContentTransitions>

        </Frame>
    </Grid>
</Page>
